import { Text, View } from 'react-native';

const Wordmark = () => {
    return (
        <View>
            <Text className={`text-2xl font-bold text-white`}>Wordmark</Text>
        </View>
    );
};

export default Wordmark;
