package tests

/* TEST CASES:
test createEvent works
test createEvent fails on:
	invalid starttime/endtime
	invalid name/preview/content/location
	invalid eventtype (not open/membersOnly)

test createEventSeries works
test createEventSeries fails on:
	the stuff from above
	invalid recurringType (ex. Annually)
	invalid separationCount
	invalid dayofweek, weekofmonth, dayofmonth

test getEvent(by ID)

test updateEvent works
test updateEvent fails on:

*/
